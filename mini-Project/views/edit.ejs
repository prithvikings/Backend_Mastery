<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a href="/logout" class="bg-red-500 rounded-md inline-block px-3 py-2 text-sm mb-5 font-medium hover:bg-red-700">
                Logout
            </a>
        </div>

        <h5 class="mb-5">Edit Your post</h5>

        <!-- Post Creation Form -->
        <form action="/update/<%=post._id%>" method="post">
            <textarea name="content" class="w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md outline-none p-3 pl-1 block" placeholder="What's on your mind?">
                <%= post.content %>
            </textarea>
            <input type="submit" class="px-3 py-2 bg-yellow-500 rounded-md mt-2 w-40 font-medium text-black text-sm hover:cursor-pointer hover:bg-yellow-700" value="Update Your Post">
        </form>

    </div>
</body>
</html>
